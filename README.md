# Noise-Filtering-System-for-Audio-Signals-with-Spectrum-Display
Design and implement a Noise Filtering System in Python that reads an audio signal, allows the user to select different filter modes through an interactive menu, applies Fast Fourier Transform (FFT) to analyze the frequency spectrum, and displays the original vs filtered signal graphically using NumPy and Matplotlib."
